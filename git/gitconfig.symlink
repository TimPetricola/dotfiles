[user]
	name = Tim Petricola
	email = tim.petricola@pm.me

[github]
  user = TimPetricola

[color]
  diff = auto
  status = auto
  branch = auto
  ui = true

[pager]
  diff = diff-so-fancy | less --tabs=1,5 -RFX
  show = diff-so-fancy | less --tabs=1,5 -RFX

[pull]
	rebase = true

[core]
  excludesfile = ~/.gitignore
  editor = cursor --wait

[init]
	defaultBranch = main
